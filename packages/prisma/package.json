{
  "name": "@briefly/prisma",
  "version": "1.0.0",
  "packageManager": "yarn@3.2.0",
  "repository": "https://github.com/AsyncPlanningPoker/BrieflyPlanningPoker",
  "license": "GPL-3.0 License",
  "private": true,
  "main": "dist/index",
  "scripts": {
    "build": "echo \"Building project @briefly/prisma...\" && tsc -b --verbose",
    "prisma:up": "echo \"Generating prisma artifacts...\" && yarn prisma generate && yarn build",
    "db:up": "yarn prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "5.2.0",
    "zod": "3.21.1"
  },
  "devDependencies": {
    "prisma": "5.2.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.6.2",
    "zod-prisma-types": "2.7.9"
  }
}
