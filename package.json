{
  "name": "briefly",
  "version": "1.0.0",
  "packageManager": "yarn@3.2.0",
  "repository": "https://github.com/AsyncPlanningPoker/BrieflyPlanningPoker",
  "license": "GPL-3.0 License",
  "private": true,
  "engines": {
    "node": "16.14.0"
  },
  "scripts": {
    "packageManager": "yarn@3.2.0",
    "postinstall": "if [ $CLIENT_ENV ]; then yarn postinstall-client; elif [ $SERVER_ENV ]; then yarn postinstall-server; else echo no environment detected, please set CLIENT_ENV or SERVER_ENV; fi",
    "postinstall-client": "cd packages/front && yarn install && yarn build",
    "postinstall-server": "cd packages/api && yarn install"
  },
  "workspaces": [
    "packages/front"
  ]
}
