{
  "name": "briefly",
  "version": "1.0.0",
  "repository": "https://github.com/AsyncPlanningPoker/BrieflyPlanningPoker",
  "license": "GPL-3.0 License",
  "private": true,
  "engines": {
    "node": "18"
  },
  "scripts": {
    "prep": "yarn install && yarn prisma:up",
    "all": "yarn prep && yarn workspaces foreach install",
    "test:integration": "vitest -c ./vitest.config.integration.ts --run --coverage"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "dotenv": "^16.0.2"
  },
  "packageManager": "yarn@4.0.2",
  "devDependencies": {
    "@types/supertest": "^2.0.16",
    "@vitest/coverage-istanbul": "^1.0.4",
    "@vitest/coverage-v8": "^1.0.4",
    "supertest": "^6.3.3",
    "vitest": "^1.0.4"
  }
}
